# Codacy configuration — suppress false positives
# https://docs.codacy.com/repositories-configure/configuring-codacy-using-a-config-file/
---
exclude_paths:
  - ".github/workflows/**"
  # Thin System-forwarding GlobalAlloc used only to count heap allocations.
  - "tests/propagate_into_alloc.rs"
